"""api.utils

Utility functions are bits of standalone logic that act on a single thing (no reliance on database,
other model behavior, etc.). Functions should generally only be added as utilities if they are
used in multiple places (including tests).
"""
